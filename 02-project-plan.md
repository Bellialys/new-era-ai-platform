# 02 - План проекта

## Назначение файла

Этот файл описывает общий поэтапный план разработки проекта **Новая эпоха**.

Главная задача файла - показать, как проект должен двигаться от документации и простого интерфейса к стабильной AI-платформе сравнения моделей.

Главный источник порядка версий - `14-roadmap.md`.

Если этот файл конфликтует с `14-roadmap.md`, правильным считается `14-roadmap.md`.

---

# Главный принцип разработки

Проект разрабатывается без хаоса и без попытки сразу создать большую систему.

Основные правила:

1. Сначала создаётся понятная документация.
2. Потом создаётся базовая Next.js основа.
3. Потом делается Static UI MVP для Prompt Arena.
4. После этого подключаются OpenRouter API, Supabase, голосование и история.
5. Только после стабильного Prompt Arena добавляются Code Arena, Multi Model Battle, Judge Mode и Leaderboard.
6. Code Arena Runner и AI Team Mode добавляются только после стабильной базы, лимитов и контроля безопасности.
7. Каждый этап должен давать рабочий результат.
8. После каждого важного этапа нужно делать Git commit.
9. Новая функция не должна ломать уже работающую часть проекта.

---

# Канонический порядок разработки

Краткий порядок по `14-roadmap.md`:

| Версия | Название | Главный результат |
|---|---|---|
| `v0.1` | Project Documentation | Готова документация и структура проекта |
| `v0.2` | Next.js Base | Проект имеет базовую Next.js основу |
| `v0.3` | UI MVP | Есть интерфейс Prompt Arena без реального AI |
| `v0.4` | OpenRouter Integration | Модели дают реальные ответы через backend |
| `v0.5` | Supabase Integration | Задачи, ответы и модели сохраняются в базе |
| `v0.5.1` | Migration Sync | Репозиторий и remote Supabase migrations синхронизированы |
| `v0.5.2` | Health and Voting Foundation | `/api/health`, smoke-check, исправленная база votes |
| `v0.6` | Auth, Guest Mode and Profile | Гости, аккаунты, профиль, ограничения моделей |
| `v0.7` | Voting MVP | `/api/vote` и сохранение выбора лучшего ответа |
| `v0.8` | History MVP | Можно смотреть прошлые сравнения |
| `v0.9` | First Deploy Stabilization | Проверка production, env, smoke, UX |
| `v1.0` | Stable Prompt Arena | Первая стабильная версия MVP |
| `v1.1` | Code Arena Lite | Сравнение решений по коду без запуска кода |
| `v1.2` | Multi Model Battle | Формальные битвы моделей по одному заданию |
| `v1.3` | Judge Mode | Модель-судья оценивает ответы других моделей |
| `v1.4` | Leaderboard | Рейтинг моделей по голосам и оценкам |
| `v1.5` | Admin Panel and Limits | Управление моделями, режимами, лимитами и стоимостью |
| `v1.6` | Code Arena Runner | Безопасный запуск кода в sandbox |
| `v1.7` | Image Arena MVP | Сравнение изображений от image-моделей |
| `v2.0` | AI Team Mode | Командная работа нескольких моделей с ролями |

---

# Что считается MVP

В проекте есть два уровня MVP.

## 1. UI MVP

UI MVP - это версия `v0.3`.

Она показывает основную идею проекта без реальных AI-запросов и без базы данных.

Входит:

- главная страница;
- страница `/arena`;
- поле ввода задачи;
- выбор 2-3 mock-моделей;
- кнопка запуска сравнения;
- loading state;
- empty state;
- error state;
- mock-ответы;
- локальный UI-выбор победителя.

Не входит:

- OpenRouter API;
- Supabase;
- сохранение задач;
- сохранение ответов;
- сохранение голосов;
- история;
- production deploy.

## 2. Stable Prompt Arena MVP

Stable Prompt Arena MVP - это версия `v1.0`.

Она должна доказать главную ценность проекта: пользователь отправляет одну задачу нескольким AI-моделям, получает ответы рядом, сравнивает их, выбирает лучший вариант и может открыть результат позже.

Входит:

- реальная отправка запроса через OpenRouter API;
- получение ответов от нескольких моделей;
- отображение ответов рядом;
- сохранение задачи и ответов в Supabase;
- сохранение голоса через `/api/vote`;
- история запросов;
- базовая обработка ошибок;
- базовые лимиты;
- деплой на Vercel.

---

# Фаза 1 - Project Documentation / v0.1

## Цель

Создать понятную основу проекта до активной разработки.

## Что нужно сделать

- описать идею проекта;
- описать MVP;
- описать архитектуру;
- описать базу данных;
- описать API;
- описать безопасность;
- описать roadmap;
- описать будущие режимы;
- зафиксировать документы через Git commit.

## Результат

Есть документация, по которой можно начинать разработку.

---

# Фаза 2 - Next.js Base / v0.2

## Цель

Создать базовую Next.js основу проекта.

## Что нужно сделать

- создать `package.json`;
- добавить Next.js, React и TypeScript;
- добавить базовую конфигурацию Next.js;
- добавить ESLint;
- добавить Tailwind CSS;
- создать `src/app/layout.tsx`;
- создать `src/app/page.tsx`;
- создать стартовую страницу `/arena`;
- проверить локальный запуск после `npm install`.

## Результат

Проект имеет Next.js основу и может быть проверен локально.

## Проверка

```bash
npm install
# устанавливает зависимости и создаёт package-lock.json

npm run typecheck
# проверяет TypeScript

npm run lint
# проверяет ESLint

npm run build
# проверяет production-сборку

npm run dev
# запускает проект локально
```

---

# Фаза 3 - Static UI MVP / v0.3

## Цель

Сделать первый интерактивный интерфейс Prompt Arena без реального AI.

## Что нужно сделать

- создать форму prompt;
- добавить выбор 2-3 моделей;
- добавить валидацию prompt;
- добавить валидацию количества моделей;
- добавить mock-генерацию ответов;
- добавить empty, loading, error и success states;
- добавить карточки ответов;
- добавить локальный UI-выбор победителя;
- добавить кнопку очистки формы.

## Результат

Пользователь видит будущую механику платформы: ввод задачи, выбор моделей, запуск сравнения, mock-ответы и локальный выбор победителя.

Важно: в `v0.3` выбор победителя не сохраняется в базе. Сохранение голоса относится к `v0.6`.

---

# Фаза 4 - OpenRouter Integration / v0.4

## Цель

Добавить реальные запросы к AI-моделям через OpenRouter.

## Что нужно сделать

- добавить `OPENROUTER_API_KEY` в `.env.local`;
- создать `/api/models`;
- создать `/api/compare`;
- создать server-side OpenRouter client;
- создать allowlist моделей;
- отправлять запрос к OpenRouter только с backend;
- получить ответы от нескольких моделей;
- обработать ошибки отдельных моделей;
- не раскрывать OpenRouter key во frontend.

## Результат

Пользователь может отправить задачу нескольким AI-моделям и увидеть реальные ответы.

---

# Фаза 5 - Supabase Integration / v0.5

## Цель

Сохранять модели, задачи и ответы в Supabase PostgreSQL.

## Что нужно сделать

- создать проект Supabase;
- создать таблицы MVP;
- подключить Supabase client;
- добавить переменные окружения;
- сохранять задачи пользователя;
- сохранять ответы моделей;
- сохранять ошибки моделей;
- подготовить таблицу `votes` для следующей фазы.

## Минимальные таблицы

- `models` - список доступных моделей;
- `tasks` - задачи пользователей;
- `model_responses` - ответы моделей;
- `votes` - голоса за лучший ответ.

## Результат

Запросы и ответы больше не теряются после обновления страницы.

---

# Фаза 6 - Auth, Guest Mode and Profile / v0.6

## Цель

Сделать пользователей и гостевой режим фундаментом дальнейшего проекта.

## Что нужно сделать

- создать guest/session основу;
- добавить регистрацию и вход;
- подготовить profile MVP;
- добавить ограничения доступа к моделям;
- сохранять `tasks.user_id` или `tasks.anonymous_session_id`.

## Результат

Prompt Arena понимает user/guest контекст и готова к следующему Voting MVP.

---

# Фаза 7 - Voting MVP / v0.7

## Цель

Добавить сохранение выбора лучшего ответа.

## Что нужно сделать

- стабилизировать `/api/vote`;
- принимать `taskId`, `responseId`, `voteType = "best"`;
- проверять, что ответ относится к задаче;
- сохранять best vote в таблицу `votes`;
- использовать актуальную схему `votes.model_response_id`;
- показывать выбранный лучший ответ после сохранения.

## Результат

Пользователь может не только сравнивать ответы, но и сохранить выбор лучшего результата.

---

# Фаза 8 - History MVP / v0.8

## Цель

Добавить историю прошлых сравнений.

## Что нужно сделать

- создать страницу `/history`;
- создать страницу `/history/[taskId]`;
- создать `/api/history`;
- создать `/api/history/[taskId]`;
- загрузить список сохранённых задач;
- открыть подробный результат сравнения.

## Результат

Пользователь может вернуться к прошлым сравнениям.

---

# Фаза 9 - First Deploy Stabilization / v0.9

## Цель

Стабилизировать production deploy после аккаунтов, guest mode и истории.

## Что нужно сделать

- добавить переменные окружения в Vercel;
- выполнить production build;
- проверить OpenRouter API на production;
- проверить Supabase на production;
- проверить регистрацию, guest mode и profile;
- проверить главные пользовательские сценарии;
- проверить production logs;
- проверить стоимость запросов.

## Результат

Проект доступен как рабочий сайт и готовится к `v1.0`.

---

# Фаза 10 - Stable Prompt Arena / v1.0

## Цель

Зафиксировать первую стабильную рабочую версию MVP.

## Критерии готовности

- `/arena` работает с реальными моделями;
- AI-запросы идут только через backend;
- задачи сохраняются в Supabase;
- ответы сохраняются в Supabase;
- голоса сохраняются в Supabase;
- история открывается;
- production deploy работает;
- секреты не попали в GitHub;
- проект можно развивать дальше без переписывания основы.

---

# Что не входит в ранний MVP

До `v1.0` не добавляем:

- регистрацию пользователей;
- оплату;
- сложный Leaderboard;
- AI Team Mode;
- запуск чужого кода;
- полноценный sandbox;
- сложную админ-панель;
- расширенную аналитику;
- командную работу моделей;
- автоматическое тестирование кода;
- сложные тарифы и подписки.

Эти функции добавляются позже, когда базовая Prompt Arena уже работает стабильно.

---

# Следующий практический шаг

Текущий статус проекта по `14-roadmap.md`:

```text
v0.5.2 - Supabase, migrations and health stabilization
# текущий стабильный фундамент проекта
```

Перед переходом к следующему этапу нужно регулярно выполнять:

```bash
npm run typecheck
# проверить TypeScript

npm run lint
# проверить ESLint

npm run build
# проверить production-сборку

npm run smoke
# проверить /api/health и /api/models
```

Следующий главный этап описан в `20-auth-guest-profile-plan.md`.
