# 20 - Stage 2 Verification

## Назначение файла

Этот файл фиксирует результат этапа **v0.2 - Next.js Base**.

Цель этапа - создать базовую основу Next.js проекта, чтобы репозиторий перешёл от одной документации к рабочему каркасу приложения.

---

# Что добавлено

## Project manifest

Добавлен файл:

```text
package.json
# главный файл npm-проекта с зависимостями и командами
```

В нём есть команды:

```bash
npm run dev
# запускает локальную разработку

npm run build
# проверяет production-сборку

npm run start
# запускает production-сервер после сборки

npm run lint
# проверяет код через ESLint

npm run lint:fix
# исправляет часть ошибок ESLint автоматически

npm run typecheck
# проверяет TypeScript без сборки
```

---

# Конфигурационные файлы

Добавлены и проверены:

```text
tsconfig.json
# TypeScript-конфигурация проекта

next.config.ts
# конфигурация Next.js

eslint.config.mjs
# конфигурация ESLint

postcss.config.mjs
# конфигурация PostCSS для Tailwind CSS

next-env.d.ts
# декларации типов Next.js
```

---

# App Router структура

Добавлены базовые файлы приложения:

```text
src/app/layout.tsx
# корневой layout приложения

src/app/globals.css
# глобальные стили

src/app/page.tsx
# главная страница /

src/app/arena/page.tsx
# стартовая страница Prompt Arena /arena
```

---

# Что уже работает по структуре

## Главная страница

Путь:

```text
/
# главная страница проекта
```

На странице есть:

- название проекта;
- краткое описание идеи;
- ссылка на Prompt Arena;
- ссылка на GitHub-документацию;
- список режимов проекта;
- стек технологий.

## Prompt Arena page

Путь:

```text
/arena
# стартовая страница Prompt Arena
```

На странице есть:

- поле ввода prompt;
- выбор mock-моделей;
- кнопка запуска сравнения;
- карточки mock-ответов;
- кнопка выбора лучшего ответа;
- пометка, что реальные OpenRouter и Supabase будут добавлены позже.

---

# Что проверено вручную по коду

- `src/app/layout.tsx` импортирует `globals.css`.
- `src/app/page.tsx` использует `next/link` для внутренних переходов.
- `src/app/arena/page.tsx` использует `next/link` для перехода на главную страницу.
- Внутренняя ссылка на документацию исправлена на GitHub README.
- `.gitignore` не блокирует `next-env.d.ts`.
- `.env.local` остаётся запрещённым для Git.
- `.env.example` не содержит настоящих секретов.
- `README.md` обновлён под состояние v0.2.
- `19-development-checklist.md` обновлён под состояние v0.2.

---

# Что невозможно полностью проверить через GitHub-инструмент

Через GitHub-инструмент можно читать и изменять файлы, но нельзя выполнить полноценную локальную установку зависимостей в твоей рабочей папке.

Поэтому локально ещё нужно выполнить:

```bash
npm install
# устанавливает зависимости и создаёт package-lock.json

npm run typecheck
# проверяет TypeScript

npm run lint
# проверяет ESLint

npm run build
# проверяет production-сборку

npm run dev
# запускает проект локально
```

---

# Важное ограничение

`package-lock.json` пока отсутствует.

Это нормально до первого локального запуска:

```bash
npm install
# создаст package-lock.json
```

После этого lock-файл нужно добавить в Git:

```bash
git add package-lock.json
# добавляет lock-файл

git commit -m "Add npm lockfile"
# фиксирует точные версии зависимостей

git push
# отправляет изменения в GitHub
```

---

# Статус этапа

```text
v0.2 - Next.js Base
# структура создана
# требуется локальная установка зависимостей и проверка команд npm
```

Этап можно считать структурно выполненным, но технически полностью подтверждённым он станет после успешных команд:

```bash
npm install
npm run typecheck
npm run lint
npm run build
npm run dev
```

---

# Следующий этап

Следующий этап - **v0.3 - Static UI MVP**.

На нём нужно:

- вынести UI-блоки в компоненты;
- добавить client-side состояние;
- сделать mock-генерацию ответов после нажатия кнопки;
- добавить состояния loading, empty, error, success;
- добавить валидацию prompt;
- добавить валидацию выбора моделей;
- улучшить мобильную адаптацию;
- сделать визуальный выбор победителя.
